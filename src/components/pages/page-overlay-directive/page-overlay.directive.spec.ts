import { PageOverlayDirective } from './page-overlay.directive';

describe('PageOverlayDirective', () => {
  it('should create an instance', () => {
    const directive = new PageOverlayDirective();
    expect(directive).toBeTruthy();
  });
});
